{# Small, pure functions with no side effects #}

{% macro detect_dynamic_columns(file_data) -%}
  {# Scans data and returns namespace with flags #}
{%- endmacro %}

{% macro format_key_name(key) -%}
  {# Handles dotted keys #}
{%- endmacro %}

{% macro format_variable_value(details) -%}
  {# Formats value with empty/multiline handling #}
{%- endmacro %}

{% macro should_skip_empty_value(details) -%}
  {# Returns true if value should be skipped #}
{%- endmacro %}

{% macro render_repo_link_for_key(key, file_path, line, role) -%}
  {# Generates repository URL #}
{%- endmacro %}
